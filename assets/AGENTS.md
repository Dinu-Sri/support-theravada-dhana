# assets/

Vanilla CSS/JS. Read `.agents/docs/frontend.md`.

- No npm, no minifier. Change the file the PHP page already links.
- Booking form: `js/booking-steps.js` is current. Do not load it together with `booking.js` / `booking-steps-minimal.js`.
- Keep the gold/brown palette and existing class names unless the task is a visual redesign.
- Prefer small helper functions in the same file. `date-utils.js` is the shared date module.
